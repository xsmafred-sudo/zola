---
phase: 01-foundation
plan: 05
type: execute
wave: 2
depends_on: [01, 02, 03, 04]
files_modified: []
autonomous: true
requirements: [LIB-01]
user_setup: []

must_haves:
  truths:
    - "Users can view discovered leads in a library interface"
    - "Leads can be bulk selected and processed with batch operations"
    - "Lead data includes validation status and source attribution"
    - "Lead library supports filtering, sorting, and search capabilities"
  artifacts:
    - path: "app/library/page.tsx"
      provides: "Main lead library page component"
      exports: ["default"]
    - path: "app/library/components/lead-table.tsx"
      provides: "Table component for displaying leads with selection"
      exports: ["LeadTable"]
    - path: "app/library/components/lead-filters.tsx"
      provides: "Filtering and search component for lead library"
      exports: ["LeadFilters"]
    - path: "app/library/components/bulk-actions.tsx"
      provides: "Bulk action controls for selected leads"
      exports: ["BulkActions"]
    - path: "lib/user-store/provider.tsx"
      provides: "Extended user store with lead management capabilities"
      exports: ["UserProvider", "useUser"]
    - path: "lib/user-store/lead-types.ts"
      provides: "Type definitions for lead data and operations"
      exports: ["Lead", "LeadSource", "ValidationStatus", "LeadFilters"]
  key_links:
    - from: "app/library/page.tsx"
      to: "app/library/components/lead-table.tsx"
      via: "renders lead table with data and filters"
      pattern: "<LeadTable leads={leads} filters={filters} onSelect={handleLeadSelect} />"
    - from: "app/library/components/lead-table.tsx"
      to: "app/library/components/bulk-actions.tsx"
      via: "receives selected leads for batch operations"
      pattern: "selectedLeads prop"
    - from: "app/library/components/lead-filters.tsx"
      to: "app/library/page.tsx"
      via: "provides filter state to library page"
      pattern: "onFilterChange callback"
    - from: "lib/user-store/provider.tsx"
      to: "lib/user-store/lead-types.ts"
      via: "uses lead type definitions"
      pattern: "import type { Lead, LeadSource }"
    - from: "lib/discovery-agents/*-agent.ts"
      to: "lib/user-store/provider.tsx"
      via: "stores discovered leads through user store"
      pattern: "userStore.addLead"
---

<objective>
Create lead library with bulk operations for managing discovered prospects

Purpose: Provide users with an interface to view, filter, and manage discovered leads from LinkedIn and Crunchbase with bulk processing capabilities
Output: Functional lead library interface with display, filtering, selection, and bulk operations
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/01-foundation-CONTEXT.md
@lib/user-store/provider.tsx
@lib/discovery-agents/orchestrator.ts
@app/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define lead data types and store extensions</name>
  <files>lib/user-store/lead-types.ts, lib/user-store/provider.tsx</files>
  <action>Create Lead type definition with fields for lead data (name, title, company, location, contact info), source attribution (LinkedIn/Crunchbase), validation status, and metadata. Extend user store provider with methods for adding leads, updating lead data, bulk lead operations, and lead querying capabilities. Ensure proper TypeScript integration with existing user profile types.</action>
  <verify>npm run type-check passes; lead-types.ts exports Lead, LeadSource, ValidationStatus, LeadFilters interfaces; user store provider has addLead, updateLead, bulkUpdateLeads, getLeads methods; proper TypeScript definitions for all lead operations</verify>
  <done>Lead data types defined and user store extended with lead management capabilities</done>
</task>

<task type="auto">
  <name>Task 2: Create lead library UI components</name>
  <files>app/library/page.tsx, app/library/components/lead-table.tsx, app/library/components/lead-filters.tsx, app/library/components/bulk-actions.tsx</files>
  <action>Build lead library interface with: 1) Lead table displaying leads with checkboxes for selection, 2) Filtering/sidebar component for source, validation status, date ranges, and search, 3) Bulk actions component for exporting, tagging, or deleting selected leads, 4) Main page component orchestrating the library layout with proper loading and error states. Use shadcn/ui components for tables, buttons, forms, and dialogs.</action>
  <verify>npm run dev runs without errors; visiting /library shows library interface; lead table displays columns for name, title, company, source, validation status; filter component controls table display; bulk actions enabled when leads selected</verify>
  <done>Lead library UI components created with table, filters, and bulk action capabilities</done>
</task>

<task type="auto">
  <name>Task 3: Implement lead data persistence and retrieval</name>
  <files>lib/user-store/api.ts, lib/user-store/provider.tsx</files>
  <action>Extend user store API with Supabase operations for lead management: create leads table if not exists, implement CRUD operations for leads, add indexing for efficient querying, and ensure proper RLS policies. Connect provider methods to API implementations for adding, updating, deleting, and querying leads with proper error handling and loading states.</action>
  <verify>npm run type-check passes; user store API has createLead, updateLead, deleteLead, getLeads functions; provider methods call API functions; Supabase schema includes leads table with proper indexes; proper error handling for database operations</verify>
  <done>Lead data persistence layer implemented with Supabase integration</done>
</task>

<task type="auto">
  <name>Task 4: Add lead library navigation and discovery integration</name>
  <files>app/layout.tsx, app/components/chat/use-discovery.ts</files>
  <action>Add lead library link to main navigation in layout component. Update discovery agent integration to automatically add discovered leads to the lead library with appropriate source tagging and initial validation status. Ensure proper navigation between chat interface and lead library with contextual awareness of user's current location.</action>
  <verify>npm run dev runs without errors; layout shows navigation link to /library; discovery agents call userStore.addLead with source metadata; navigation works between chat and library pages; leads appear in library after discovery</verify>
  <done>Lead library accessible from main navigation and populated by discovery agents</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary          | Description                                   |
| ----------------- | --------------------------------------------- |
| user→library      | User interactions with lead library interface |
| library→api       | Library API calls to backend services         |
| api→database      | Lead data storage and retrieval operations    |
| discovery→library | Discovery agents storing leads in library     |

## STRIDE Threat Register

| Threat ID | Category | Component                  | Disposition | Mitigation Plan                                                                            |
| --------- | -------- | -------------------------- | ----------- | ------------------------------------------------------------------------------------------ |
| T-05-01   | S        | Lead data exposure         | mitigate    | Implement proper RLS policies on leads table; ensure users only see their own leads        |
| T-05-02   | T        | Lead data integrity        | mitigate    | Validate lead data before storage; use database constraints for required fields            |
| T-05-03   | I        | SQL injection              | mitigate    | Use Supabase parameterized queries; validate all inputs to lead operations                 |
| T-05-04   | R        | Unauthorized lead access   | mitigate    | Middleware and API endpoints validate user authentication before lead operations           |
| T-05-05   | D        | Data loss                  | mitigate    | Implement soft deletes for leads; regular database backups; confirm bulk delete operations |
| T-05-06   | E        | Client-side data tampering | mitigate    | All lead modifications validated by backend; client cannot bypass validation               |

</threat_model>

<verification>
Overall phase checks:
- npm run dev starts successfully
- Visit /library shows lead library interface
- Lead table displays leads with proper columns and data
- Filtering controls affect displayed leads appropriately
- Bulk actions work on selected leads
- Discovery agents add leads to library with source metadata
- Navigation works between chat and library interfaces
</verification>

<success_criteria>

- Users can view discovered leads in a library interface
- Leads can be bulk selected and processed with batch operations
- Lead data includes validation status and source attribution
- Lead library supports filtering, sorting, and search capabilities
- Lead library is accessible from main navigation
- Discovery agents properly store discovered leads in library
- All lead library code follows TypeScript and Next.js best practices
  </success_criteria>

<output>
After completion, create .planning/phases/01-foundation/01-05-SUMMARY.md
</output>
